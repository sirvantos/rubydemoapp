## Customize deployment commands
deployment:
  production:
    branch: master
    commands:
      - bundle exec rake production deploy --trace
  staging:
    branch: develop
    commands:
      - bundle exec rake staging deploy --trace
