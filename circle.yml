## Customize deployment commands
deployment:
  production:
    branch: master
    heroku:
        appname: rubytestapp-production
    commands:
      - bundle exec rake production deploy
  staging:
    branch: develop
    heroku:
        appname: rubytestapp-staging
    commands:
      - bundle exec rake staging deploy