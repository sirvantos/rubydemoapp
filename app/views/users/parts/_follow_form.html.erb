<% unless current_user?(@user) %>
	<div id="follow_form">
		<% if current_user.following?(@user) %>
			<%= render 'users/parts/unfollow' %>
		<% else %>
			<%= render 'users/parts/follow' %>
		<% end %>
	</div>
<% end %>